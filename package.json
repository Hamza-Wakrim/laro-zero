{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently \"php artisan serve\" \"php artisan queue:listen --tries=1\" --names=server,queue --kill-others"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
